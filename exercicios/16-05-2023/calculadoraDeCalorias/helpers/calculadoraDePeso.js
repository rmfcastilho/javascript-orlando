const calculadoraDePeso = (kcal) => {
  // seu codigo aqui.
  // A função deve receber um número e retornar um número que é a conversão de kcal para kg
  // o número retornado deve ter 2 casas decimais
};


module.exports = { calculadoraDePeso };
