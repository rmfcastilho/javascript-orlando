const transformadorDeNumero = (kcal) => {
  // seu código aqui
  // a função deve retornar pegar uma string e limpar ela, removendo o 'kcal'
  // se atente para retornar um número e não uma string!
};

module.exports = { transformadorDeNumero };
